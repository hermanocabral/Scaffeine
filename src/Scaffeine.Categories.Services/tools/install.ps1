param($installPath, $toolsPath, $package, $project)

scaffold scaffr.backend.for Category

#add-migration "Caregories Added" -projectName $dataProjectName

#update-database -projectName $dataProjectName
